#include <lib/out.c>

//int cmd_help(char *args[], int argslen) {
int cmd_help() {
	int len = 58;
	println("You're inside the handler function for the 'help' command!", len);
	len = 30;
	println("Someone should implement me :(", len);
	return 0;
}
